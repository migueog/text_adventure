# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# PostgreSQL database connection string
# This is the primary connection for your application
#
# Format: postgresql://username:password@host:port/database?options
# Example (Local): postgresql://postgres:password@localhost:5432/campaign_manager
# Example (Neon): postgresql://user:pass@ep-xyz.us-east-2.aws.neon.tech/campaign_manager?sslmode=require
# Example (Supabase): postgresql://postgres:pass@db.xyz.supabase.co:5432/postgres
#
# IMPORTANT: For production deployments (Vercel, etc.), set this as an 
# environment variable in your hosting platform, not in .env files
DATABASE_URL=postgresql://user:password@localhost:5432/campaign_manager

# =============================================================================
# DATABASE HOSTING PROVIDER OPTIONS
# =============================================================================

# OPTION 1: Neon (RECOMMENDED for serverless/Vercel)
# -----------------------------------------------------------------------------
# Free Tier: 10 GB storage, 3 projects, unlimited queries
# Built-in connection pooling, auto-scaling, serverless-optimized
# Sign up: https://neon.tech
#
# Setup Steps:
# 1. Create account at neon.tech
# 2. Create new project (e.g., "campaign-manager-dev")
# 3. Copy the "Pooled connection" string from Connection Details
# 4. Paste it as DATABASE_URL above
#
# Example Neon URL:
# DATABASE_URL=postgresql://user:password@ep-abc123.us-east-2.aws.neon.tech/campaign_manager?sslmode=require

# OPTION 2: Supabase (Good for additional features)
# -----------------------------------------------------------------------------
# Free Tier: 500 MB database, 2 GB bandwidth/month
# Includes: Built-in auth, realtime subscriptions, storage
# Sign up: https://supabase.com
#
# Setup Steps:
# 1. Create account at supabase.com
# 2. Create new project (e.g., "campaign-manager-dev")
# 3. Go to Settings > Database > Connection Pooling
# 4. Use the "Transaction" mode connection string
# 5. Paste it as DATABASE_URL above
#
# Example Supabase URL (Pooler mode):
# DATABASE_URL=postgresql://postgres.xyz:password@aws-0-us-east-1.pooler.supabase.com:6543/postgres

# OPTION 3: Railway (All-in-one platform)
# -----------------------------------------------------------------------------
# Free Tier: $5 credit/month (enough for small projects)
# Can host both database and application
# Sign up: https://railway.app
#
# Setup Steps:
# 1. Create account at railway.app
# 2. Create new PostgreSQL database
# 3. Copy the PostgreSQL connection URL
# 4. Paste it as DATABASE_URL above

# OPTION 4: Local Development
# -----------------------------------------------------------------------------
# For local PostgreSQL instance:
# 1. Install PostgreSQL on your machine
# 2. Create a database: createdb campaign_manager
# 3. Update the connection string below
#
# DATABASE_URL=postgresql://postgres:yourpassword@localhost:5432/campaign_manager

# =============================================================================
# PRODUCTION ENVIRONMENT SETUP
# =============================================================================
# For Vercel or other hosting platforms:
# 
# 1. Create a SEPARATE database for production (e.g., "campaign-manager-prod")
# 2. In your hosting platform dashboard:
#    - Go to Environment Variables
#    - Add: DATABASE_URL with your production connection string
#    - Set environment to: Production
# 3. NEVER commit production credentials to git
#
# For Vercel specifically:
# - Go to Project Settings > Environment Variables
# - Add DATABASE_URL for Production environment
# - Use Neon's "Pooled connection" for best performance

# =============================================================================
# CONNECTION POOLING CONFIGURATION (Optional)
# =============================================================================
# These are handled automatically by the client.ts configuration
# Only override if you need custom settings
#
# DB_POOL_MAX=10                    # Maximum connections in pool
# DB_POOL_IDLE_TIMEOUT=30000        # Milliseconds before idle connection closes
# DB_POOL_CONNECTION_TIMEOUT=2000   # Milliseconds before connection attempt fails

# =============================================================================
# MIGRATION CONFIGURATION (Optional)
# =============================================================================
# Only needed if you want to run migrations against a different database
# By default, migrations use DATABASE_URL
#
# MIGRATION_DATABASE_URL=postgresql://user:password@host:port/database
